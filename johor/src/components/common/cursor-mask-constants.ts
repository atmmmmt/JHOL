/** Radius (px) for hero / blend mask. */
export const MASK_RADIUS = 120;

/** Smaller radius for brand-logo grid hover. */
export const BRAND_LOGO_MASK_RADIUS = 75;

/** Footer hover mask radius. */
export const FOOTER_MASK_RADIUS = 160;

/** Diameter (px) for CTA / link hover cursor. */
export const CTA_CURSOR_SIZE = 100;

/** Diameter (px) for package-card hover cursor. */
export const COURSE_CARD_CURSOR_SIZE = 58;

/** Diameter (px) for portfolio image/card hover cursor. */
export const WORK_CURSOR_SIZE = 116;

/** Header: nav links local clip-path radius. */
export const HEADER_LINK_MASK_RADIUS = 52;

/** Header: logo spotlight ring radius. */
export const HEADER_LOGO_MASK_RADIUS = 80;

/** Header: language toggles local clip-path radius. */
export const HEADER_LANG_MASK_RADIUS = 40;

/** Header: scaled custom cursor over nav links + language. */
export const HEADER_NAV_CURSOR_SIZE = 72;
